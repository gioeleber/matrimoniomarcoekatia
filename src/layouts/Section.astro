---
interface Props {
  id: string
	title: string;
}

const { title, id } = Astro.props;
---

<section id={id} class="xl:w-[64rem] md:mx-auto py-10 md:py-20 px-6 lg:px-4 xl:px-0">
  <h2 class="font-serif text-2xl md:text-4xl font-bold mb-5">{title}</h2>
  <slot />
</section>